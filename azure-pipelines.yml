# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
  branches:
    include:
      - main
      - develop
      - feature/*

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  SolutionPath: 'src/Remeter.sln'
  MainProjectPath: 'src/Remeter/Portal/Server/Remeter.Portal.Server.csproj'

steps:
- script: |
    echo "##vso[task.setvariable variable=deploymentSlot]dev"
  name: DefaultTarget

- script: |
    echo "##vso[task.setvariable variable=deploymentSlot]staging"
  name: StagingTarget
  condition: eq(variables['Build.SourceBranch'], 'refs/heads/main')

- script: |
    echo "##vso[task.setvariable variable=deploymentSlot]int"
  name: IntegrationTarget
  condition: eq(variables['Build.SourceBranch'], 'refs/heads/develop')

- script: |
    echo "##vso[task.setvariable variable=deploymentSlot;isOutput=true]$DEPLOYMENTSLOT"
  name: DeploymentTarget

- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: 5.0.x

- task: DotNetCoreCLI@2
  displayName: Restore
  inputs:
    command: restore
    projects: '$(SolutionPath)'

- task: DotNetCoreCLI@2
  displayName: Build
  inputs:
    command: build
    projects: '$(SolutionPath)'
    arguments: '--configuration $(buildConfiguration)'

- task: DotNetCoreCLI@2
  displayName: 'Publish and zip'
  inputs:
    command: publish
    publishWebProjects: False
    projects: '$(MainProjectPath)'
    arguments: '--configuration $(BuildConfiguration) --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: True

- task: AzureWebApp@1
  displayName: 'Deploy Azure Web App'
  inputs:
    azureSubscription: '$(azureSubscription)'
    appName: 'remeter-portal'
    SlotName: '$(DeploymentTarget.deploymentSlot)'
    appType: 'webApp'
    package: $(Build.ArtifactStagingDirectory)/**/*.zip
