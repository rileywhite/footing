# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
  batch: true
  branches:
    include:
      - develop
      - feature/*

pool:
  vmImage: 'ubuntu-latest'

variables:
  azureContainerRegistryName: 'crvemotueus2'
  azureContainerRegistryUrl: '$(azureContainerRegistryName).azurecr.io'
  dockerComposeFile: 'src/docker-compose.yml'
  timestamp: $(Get-Date -Format yyyyMMddTHHmmssfffffffZ)
  vemotuappChartPath: 'src/vemotuapp'
  vemotuappChartFilePath: '$(vemotuappChartPath)/Chart.yaml'

steps:
- script: |
    echo "##vso[task.setvariable variable=versionPrefix]$(cat src/version)"
  name: ReadVersionFromFile

- script: |
    echo "##vso[task.setvariable variable=alphaOrBeta]alpha.$(Build.SourceBranchName)"
  name: DefaultToAlpha

- script: |
    echo "##vso[task.setvariable variable=alphaOrBeta]beta"
  name: UseBetaForDevelop
  condition: eq(variables['Build.SourceBranch'], 'refs/heads/develop')

- script: |
    echo "##vso[task.setvariable variable=configurationDecision]Debug"
  name: DefaultToDebug

- script: |
    echo "##vso[task.setvariable variable=configurationDecision]Release"
  name: UseReleaseForDevelop
  condition: eq(variables['Build.SourceBranch'], 'refs/heads/develop')

- script: |
    echo "##vso[task.setvariable variable=configuration;isOutput=true]$CONFIGURATIONDECISION"
  name: SetConfiguration

- powershell: |
    $date=$(Get-Date -Format yyyyMMddTHHmmssfffffffZ);
    Write-Host "##vso[task.setvariable variable=timestamp]$date"
  name: SetTimestamp

- script: |
    echo "##vso[task.setvariable variable=version;isOutput=true]$VERSIONPREFIX-$ALPHAORBETA.$(Build.BuildId)"
  name: SetVersion

- script: |
    echo "##vso[task.setvariable variable=fullVersion;isOutput=true]$(SetVersion.version)+$TIMESTAMP"
  name: SetFullVersion

- script: |
    echo "##vso[task.setvariable variable=assemblyVersion;isOutput=true]$VERSIONPREFIX.$(Build.BuildId)"
  name: SetAssemblyVersion

- powershell: |
    $line = Get-Content $(vemotuappChartFilePath) | Select-String version | Select-Object -ExpandProperty Line
    $content = Get-Content $(vemotuappChartFilePath)
    $content | ForEach-Object {$_ -replace $line,"version: $(SetVersion.version)"} | Set-Content $(vemotuappChartFilePath)
  name: SetChartVersion

- powershell: |
    $line = Get-Content $(vemotuappChartFilePath) | Select-String appVersion | Select-Object -ExpandProperty Line
    $content = Get-Content $(vemotuappChartFilePath)
    $content | ForEach-Object {$_ -replace $line,"appVersion: $(SetFullVersion.fullVersion)"} | Set-Content $(vemotuappChartFilePath)
  name: SetChartAppVersion

- task: DockerCompose@0
  name: DockerComposeBuild
  inputs:
    containerregistrytype: 'Azure Container Registry'
    azureSubscription: '$(azureSubscription)'
    azureContainerRegistry: '{"loginServer":"crvemotueus2.azurecr.io", "id" : "/subscriptions/$(azureSubscriptionUuid)/resourceGroups/$(azureCRResourceGroup)/providers/Microsoft.ContainerRegistry/registries/crvemotueus2"}'
    dockerComposeFile: 'src/docker-compose.yml'
    action: 'Build services'
    arguments: '--build-arg version=$(SetFullVersion.fullVersion) --build-arg assembly_version=$(SetAssemblyVersion.assemblyVersion) --build-arg configuration=$(SetConfiguration.configuration)'
    dockerComposeFileArgs: |
      DOCKER_IMAGE_VERSION_TAG=$(SetVersion.version)
    includeLatestTag: false

- task: DockerCompose@0
  condition: in(variables['Build.Reason'], 'Manual', 'IndividualCI', 'BatchedCI', 'Schedule')
  name: DockerComposePush
  inputs:
    containerregistrytype: 'Azure Container Registry'
    azureSubscription: '$(azureSubscription)'
    azureContainerRegistry: '{"loginServer":"crvemotueus2.azurecr.io", "id" : "/subscriptions/$(azureSubscriptionUuid)/resourceGroups/$(azureCRResourceGroup)/providers/Microsoft.ContainerRegistry/registries/crvemotueus2"}'
    dockerComposeFile: 'src/docker-compose.yml'
    action: 'Push services'
    dockerComposeFileArgs: |
      DOCKER_IMAGE_VERSION_TAG=$(SetVersion.version)
    includeLatestTag: false

- task: HelmInstaller@0
  inputs:
    helmVersion: '3.4.1'
    installKubectl: true
    kubectlVersion: '1.19.4'

- task: HelmDeploy@0
  inputs:
    azureSubscriptionForACR: 'Azure subscription 1(ce6a314f-4d4b-43d3-bbb9-195fdc4939d9)'
    azureResourceGroupForACR: 'rg-vemotu-eus2-001'
    azureContainerRegistry: 'crvemotueus2.azurecr.io'
    command: 'save'
    chartNameForACR: 'vemotuapp:$(SetVersion.version)'
    chartPathForACR: '$(vemotuappChartPath)'
