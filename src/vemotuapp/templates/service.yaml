apiVersion: v1
kind: Service
metadata:
  name: {{ include "vemotuapp.fullname" . }}
  labels:
    {{- include "vemotuapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vemotuapp.selectorLabels" . | nindent 4 }}
