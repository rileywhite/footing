apiVersion: v1
kind: Service
metadata:
  name: {{ include "footingapp.fullname" . }}
  labels:
    {{- include "footingapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "footingapp.selectorLabels" . | nindent 4 }}
